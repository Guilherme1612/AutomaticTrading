---
task: ticker-mechanism-universe-agents
date: 2026-06-29
slug: ticker-mechanism-universe-agents
description: Make Ticker symbol + Name cells on /universe clickable to /ticker/{symbol} (same mechanism as /ticker page) — the symbol is the link; consider linking Name too. Skip already-linked Actions column.
---

# Quick Task: Ticker link on universe page

## Goal

Make the **Ticker** and **Name** columns in the /universe table clickable to `/ticker/{symbol}`, matching the existing "View data →" / "View memo →" pattern (and same as /ticker page internal links). Also extend the Agents page: the hero `current_ticker` / `last_analyzed_ticker` text and the `next_ticker` text are plain text — wrap them as `<a href="/ticker/{sym}">` when a ticker is known, so the operator can click straight through to the workspace.

## Files to change

1. `pmacs/web/templates/universe.html` — wrap the `{{ t.symbol }}` cell (line 87) and the `{{ t.name }}` cell (line 88) in `<a href="/ticker/{{ t.symbol }}">`. Use existing classes for hover (accent + underline); keep font-mono + font-medium on the symbol. Don't touch the Actions column (already linked).
2. `pmacs/web/templates/agents.html` — at lines 34, 36, 47: when `current_ticker` / `last_analyzed_ticker` / `next_ticker` is set, wrap the `<span id="current-ticker">` / `<span id="next-ticker">` text inside an `<a href="/ticker/{...}">`. Keep the same `id` and `class` on the inner span so existing JS selectors keep working. Use accent hover + underline. Don't break the JS in the file that references these ids.

## Constraints

- No new backend routes or schemas.
- No JS changes — anchor tags only.
- Keep keyboard accessibility (`href` is already focusable; no need for `tabindex`).
- The "current-ticker" span appears 3 times (lines 34, 36, 38); only wrap the populated branches (34 + 36). Branch 38 is the empty placeholder — leave untouched.
- Preserve all existing id attributes — the result-cards / carousel / ticker-history JS reads `#current-ticker` and `#next-ticker`.

## Verify

- Server is offline (operator hasn't started it); the changes are pure HTML — `python -c "import jinja2; jinja2.Environment().parse(open('pmacs/web/templates/universe.html').read())"` must not raise, and same for agents.html.
- `git diff --stat` shows only the two template files.

## Done

- Both templates render the new anchors.
- No JS / backend changes.
- Commit message: `fix(web): link ticker symbol + name on universe; wrap hero tickers on agents`
