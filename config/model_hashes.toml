# Model hashes for local GGUF backends (llama_server, ollama).
# When using API backends (anthropic, openai, openrouter), this file is irrelevant
# and hash verification is automatically skipped.
# To add a local model: run ops/compute_model_hash.sh <model.gguf>

[gguf]
